PRODUCTION=1
DEBUG=0

# TODO review these
ALLOWED_HOSTS=*

# Without this http://localhost gets redirected to https://uwsgi:9000 and
# in production it doesn't matter as we're not listening on port 80 anyways
SECURE_SSL_REDIRECT=0
